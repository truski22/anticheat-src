# Authentication required — anonymous connections are rejected.
# Credentials are generated at startup from MQTT_USER/MQTT_PASSWORD env vars.
listener 1883
allow_anonymous false
password_file /mosquitto/config/passwd
