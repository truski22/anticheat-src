<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.chessfraud</groupId>
        <artifactId>chessfraud-libs</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>ws-lib</artifactId>
    <packaging>jar</packaging>
    <name>Chessfraud WebSocket Bootstrap (legacy)</name>
    <description>
        DEPRECATED / TRANSITIONAL: standalone Tyrus WebSocket server bootstrap used by the
        gateway's current javax.websocket endpoint. To be retired once the gateway migrates
        onto spring-boot-starter-websocket, at which point this whole module should be
        deleted rather than refactored further. See the module README before adding
        anything new here.

        The javax.websocket-api / tyrus 1.13 dependencies below are intentionally kept
        alongside the newer jakarta.websocket-api 2.1.1 line: the gateway's endpoint code
        still compiles against the javax namespace, and downgrading or removing either
        line without also migrating that code will break the gateway build.
    </description>

    <dependencies>
        <!-- Jakarta WebSocket API (server-side annotations: @ServerEndpoint, @OnMessage, ...) -->
        <dependency>
            <groupId>jakarta.websocket</groupId>
            <artifactId>jakarta.websocket-api</artifactId>
            <version>2.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.tyrus</groupId>
            <artifactId>tyrus-server</artifactId>
            <version>2.1.1</version>
        </dependency>

        <!-- Legacy javax.websocket line: still required by the gateway's endpoint code today. -->
        <dependency>
            <groupId>javax.websocket</groupId>
            <artifactId>javax.websocket-api</artifactId>
            <version>1.1</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.glassfish.tyrus.bundles</groupId>
            <artifactId>tyrus-standalone-client</artifactId>
            <version>1.13</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.tyrus</groupId>
            <artifactId>tyrus-container-grizzly-client</artifactId>
            <version>1.13</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.tyrus</groupId>
            <artifactId>tyrus-server</artifactId>
            <version>1.13</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.tyrus</groupId>
            <artifactId>tyrus-container-grizzly-server</artifactId>
            <version>1.13</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.tyrus</groupId>
            <artifactId>tyrus-core</artifactId>
            <version>1.13</version>
        </dependency>

        <!-- Servlet API required by the Tyrus/Grizzly container at compile time -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>5.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>tomcat-websocket-client-api</artifactId>
            <version>10.1.0</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test-only: a concrete SLF4J binding so test output is visible.
             Consumers supply their own binding; this module never forces one. -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
